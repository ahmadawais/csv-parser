# Use Travis CI's faster Docker-based infrastructure.
sudo: false

# # PHP
# language: php

# # versions
# php:
#   - 5.4
#   - 5.5
#   - 5.6
#   - hhvm

# # what to do
# before_script:
#   - composer self-update
#   - composer install --prefer-source --no-interaction --dev

# # unit testing + config attribute to ensure it picks up the testing bootstrap file
# script: phpunit --configuration phpunit.xml

language: php

php:
  - 5.3
  - 5.4

before_script:
  - curl -s http://getcomposer.org/installer | php -- --quiet
  - php composer.phar install --dev

script:
  - vendor/bin/phpunit --coverage-text -c phpunit.xml